# Copia este archivo a .env y ajusta la URL de la base de datos si usas Postgres/MySQL.
# Por defecto usamos SQLite en archivo local.
DATABASE_URL=sqlite:///events.db
FLASK_ENV=development
FLASK_DEBUG=1
HOST=127.0.0.1
PORT=5000
